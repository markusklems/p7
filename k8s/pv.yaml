kind: PersistentVolume
apiVersion: v1
metadata:
  name: database-pv
  labels:
    kubernetes.io/cluster-service: "true"
    type: local
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "$HOME/k8s/storage/database"
